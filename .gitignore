# Environment
.env
.env.local
.env.*.local
!.env.example

# Node / Next.js
node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo
.npm
.pnpm-debug.log*
.yarn/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.eggs/
pip-log.txt
pip-delete-this-directory.txt

# Alembic — keep migrations, ignore local sqlite if any
*.sqlite
*.sqlite3
*.db

# Volume-probe runtime output (generated data, not source)
backend/reports/

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Coverage / test artifacts
coverage/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# Claude Code local overrides (project-level shared settings.json IS tracked)
.claude/settings.local.json

# Smoke / scratch scripts (local-only, not for CI or prod)
backend/scripts/smoke_*.py

# Local planning / research scratch (local-only, not tracked)
notes/

# Claude Design handoff bundle — reference only, not part of the app.
# Ignore the heavy bundle subdir (videos/screenshots) but keep the dir itself
# trackable so the redesign REVIEW log travels with the branch.
design-handoff/the-garage-ai/
