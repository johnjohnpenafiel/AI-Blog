# Page — /login

- **Sources:** `frontend/src/app/(auth)/login/page.tsx`, `login-form.tsx`

## Elements

```
Login Page (full-screen bg + grid)
└── Login Card (centered surface panel, corner tick top-left)
    ├── Card Header        ← "THE GARAGE AI / ADMIN"
    ├── Email Field        ← label + input
    ├── Password Field     ← label + input
    ├── Sign In Button     ← primary, full flow
    └── Error Line         ← "Invalid email or password."
```

No registration, no forgot-password — by design.
