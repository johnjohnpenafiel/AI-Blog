# Phase 3 — Quality & Voice — report (2026-06-03)

Branch: `automated-work` (never touched `main`).

## generation-evals — shipped 2026-06-03

**Merge commit:** 2b844d1
**Plan-removal commit:** eec3f9e
**Feature branch (deleted):** feature/generation-evals (85cbc94)

### What shipped
- `services/evals.py` `evaluate_post(post)` — a cheap Haiku LLM-judge scoring three axes (POV adherence, format adherence, source grounding) → `EvalResult` (0–2 each + `passed` + `notes`). **Fails closed** (judge error → `EvalError`).
- `backend/scripts/eval_recent_posts.py` — operator QA tool: scores the most recent N posts and prints a table.

### Manual tests for the user to run
- `docker compose run --rm backend pytest` → `154 passed`.
- `docker compose run --rm backend python scripts/eval_recent_posts.py 5` (needs `ANTHROPIC_API_KEY`) → per-post scores.

### Pending human checks
- Eyeball the judge's scores against a couple of posts you'd grade yourself, to calibrate the rubric.

### Bugs / known issues / Architectural changes
- none architectural. Eval is a runnable tool, **not** wired as a hard publish gate (deferred — would be a pipeline behavioral change). Promo-classifier-accuracy harness deferred (needs labeled data).

---

## weekly-volume-testing — shipped 2026-06-03

**Merge commit:** da72306
**Plan-removal commit:** 2f51394
**Feature branch (deleted):** feature/weekly-volume-testing (6e5aefc)

### What shipped
- `services/volume_probe.py` `measure_section_supply(db)` — per-section `{raw, news}` counts, reusing the live fetch + classifier.
- `backend/scripts/volume_probe.py` — appends a timestamped record to `reports/volume-probe-results.jsonl` and prints a table.

### Manual tests for the user to run
- `docker compose run --rm backend python scripts/volume_probe.py` (needs both API keys) → a per-section supply table + a logged record. Cron it weekly (externally) to build the time series.

### Pending human checks
- none.

### Architectural changes
- **Deliberately NOT an in-process scheduler job** and **no new DB table** — realized as an externally-cron'd script writing JSONL, to stay gate-free and avoid the single-instance scheduler scaling concern. Note that choice if you fold anything into PLANNING.md.

---

# Phase 3 complete

- Features shipped: generation-evals, weekly-volume-testing
- Commits on automated-work: 8a8b02c..2f51394 (2 merges: 2b844d1, da72306)
- Test suite: **154 passed**
- Aggregated pending human checks: calibrate the eval judge against a couple of posts.
- This was the final phase of the requested run (Phases 1–3).

## How to review

- Branch: `automated-work`
- Per-feature diffs: `git log --first-parent --oneline automated-work`.
- Inspect a feature: `git show <merge-sha>` (2b844d1 / da72306).
- When satisfied with all three phases: `git checkout main && git merge automated-work` (or open a PR). **Before that**, fold the accumulated architectural changes into `PLANNING.md` + `PLANNING-decisions.md` (see the per-feature "Architectural changes" sections across the three phase reports).
- To throw it away: `git checkout main && git branch -D automated-work`.
